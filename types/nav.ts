export interface NavItem {
  title: string;
  href?: string;
  disabled?: boolean;
  external?: boolean;
}
export interface Comment {
  profilePicture: string;
  username: string;
  comment: string;
  date: string;
}
